# Module: azure-key-vault-keys

Creates Key Vault keys from a map input.

## Inputs

- key_vault_id
- keys

Each key supports type, size/curve, key options, optional validity dates, tags, and optional rotation policy.
